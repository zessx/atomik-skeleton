<?= $form = $this->form(ROOT.'clients/ajouter', null, array('class' => 'form-horizontal')) ?>
<?= Form::generateFields($form, $fields) ?>

	<hr>
	
	<div class="form-group">
		<div class="col-lg-offset-2 col-lg-4">
			<?= $form->buttons('Ajouter le client', ROOT.'clients', array('class' => 'btn btn-primary')) ?>
		</div>
	</div>
</form>