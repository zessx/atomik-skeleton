<?= $form = $this->form(ROOT.'clients/modifier/'.$client['id_client'], null, array('class' => 'form-horizontal')) ?>
<?= Form::generateFields($form, $fields, $client) ?>

	<hr>
	
	<div class="form-group">
		<div class="col-lg-offset-2 col-lg-4">
			<?= $form->buttons('Modifier le client', ROOT.'clients', array('class' => 'btn btn-primary')) ?>
		</div>
	</div>
</form>